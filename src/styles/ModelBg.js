import styled from '@emotion/styled';

export default styled.div(props => ({
  position: 'absolute',
  zIndex: 10,
  top: 0,
  left: 0,
  backgroundColor: 'hsla(0,0%,0%,.75)',
  minWidth: '100vw',
  minHeight: '100vh',
}));